[user]
	name = Ruud Bleeker
	email = rbleeker@gmail.com
[push]
	default = simple
[filter "lfs"]
	required = true
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
[lfs]
	locksverify = true
[core]
	editor = /usr/bin/vim
[credential]
	helper = store
